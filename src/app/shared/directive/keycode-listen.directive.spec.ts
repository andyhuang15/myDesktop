import { KeycodeListenDirective } from './keycode-listen.directive';

describe('KeycodeListenDirective', () => {
  it('should create an instance', () => {
    const directive = new KeycodeListenDirective();
    expect(directive).toBeTruthy();
  });
});
