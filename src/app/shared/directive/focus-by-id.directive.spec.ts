import { FocusByIdDirective } from './focus-by-id.directive';

describe('FocusByIdDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusByIdDirective();
    expect(directive).toBeTruthy();
  });
});
